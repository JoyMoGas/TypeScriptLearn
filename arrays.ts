/*const arr:any = [];

arr.push('hola Mundo');
arr.push(2021);
arr.push({});

const numbers = [1, 2, 3];

numbers.push(4);
numbers.push(5);
// numbers.push('Popo');

const moreNumbers: (number | string)[] = [1, 'two', 3];

moreNumbers.push(4);
moreNumbers.push('5');

const names = ['thor', 'wanda', 'tony'];

names.forEach(name => {
    console.log(name.toUpperCase());
});
*/

/* Objetos */

let channel = { name: 'webtoriales'};

//channel.suscribers = 1700;
//channel = { name: true };
//channel = { name: 'JoyMoGas' };
//channel.name = '40';

//let obj: object;

//obj = { hola: 'mundo'};
//obj = { test: false };

//obj = [1 ,2, 3];

//let superHero: { name: string, age: number, superpowers: string[]};

//superHero = {
//    name: 'wanda',
//    age: 33,
//    superpowers: ['usar magia', 'volar']
//};

//superHero.superpowers.push('telequinesis');

